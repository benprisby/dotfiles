# Set up Homebrew environment.
eval "$(/opt/homebrew/bin/brew shellenv)"

# Make Qt available.
export PATH="${HOME}/Qt/6.7.1/macos/bin:${PATH}"

# Make local user installations available.
export PATH="${HOME}/.local/bin:${PATH}"
